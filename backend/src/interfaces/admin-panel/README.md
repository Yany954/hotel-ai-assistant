# Admin panel

Content management for rooms and contacts data, plus user provisioning (create accounts, assign
role, revoke access). No self-registration anywhere in the system — this is the only place
accounts are created.

Also owns: 30-day conversation history purge job (see docs/system-boundary.md), audit log view
for escalation-path answers.
