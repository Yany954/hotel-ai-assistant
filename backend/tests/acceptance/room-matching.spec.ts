// Acceptance tests mirror docs/requirements/room-matching.md 1:1.
// Example (pseudocode, fill in with your test runner):
//
// test("returns only rooms matching ALL stated constraints", ...)
// test("says so explicitly when no room matches", ...)
// test("does not exclude rooms based on unmentioned attributes", ...)
