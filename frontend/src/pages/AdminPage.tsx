// Admin-facing screen: edit room/contact data, create staff accounts (no self-registration
// anywhere else in the system), view the audit log for escalation-path answers.
// TODO: build CRUD forms backed by backend /admin endpoints (not yet defined).
import "tailwindcss";
export function AdminPage() {
  return null;
}
